// Configuración para DESARROLLO (cuando corrés `ng serve` en tu compu).
// El backend local está en el puerto 3000.
export const environment = {
  produccion: false,
  apiBase: 'http://localhost:3000/api',
};
